en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            password:
              too_short: must be at least 8 characters long
      messages:
        confirmation: "doesn't match %{attribute}"
